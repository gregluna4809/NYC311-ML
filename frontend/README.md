# NYC Civic ML Frontend

## Environment

Local development uses `frontend/.env`:

```ini
VITE_API_BASE_URL=http://localhost:8001
```

For production deployments, set:

```ini
VITE_API_BASE_URL=http://nyc311.pulse-forge.com:8001
```
