# Notebooks

This project currently uses Python scripts rather than notebooks for data profiling, cleaning, loading, analysis, and model training.

The main workflow lives in the `scripts/` directory, while the deployed model artifact is stored in `models/`.
